Question 24: You have two buttons A & B . When you click on the A button the image will be shown and you click on the B button then your name, phone number, and email will be shown. How will you achieve this problem statement?

Hint 1:- Add the flag to show the different-2 data on click of buttons.

Hint 2:- Add text in the column for showing the name, phone number, and email.

Hint 3:- Add imageView to show the image.

Hint 4:- Add the image to the assets folder.
source code:
import 'package:flutter/material.dart';

void main() {
  runApp(ShowInfoApp());
}

class ShowInfoApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Show Image or Info',
      debugShowCheckedModeBanner: false,
      home: ShowInfoScreen(),
    );
  }
}

class ShowInfoScreen extends StatefulWidget {
  @override
  _ShowInfoScreenState createState() => _ShowInfoScreenState();
}

class _ShowInfoScreenState extends State<ShowInfoScreen> {
  bool showImage = true; // flag to toggle between image and info

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Show Image or Information')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Conditional rendering based on flag
            if (showImage)
              Image.asset(
                'assets/my_image.png', // <-- Add this image in assets folder
                width: 200,
                height: 200,
              )
            else
              Column(
                children: const [
                  Text(
                    'Name: John Doe',
                    style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
                  ),
                  SizedBox(height: 8),
                  Text(
                    'Phone: +1 234 567 890',
                    style: TextStyle(fontSize: 18),
                  ),
                  SizedBox(height: 8),
                  Text(
                    'Email: john.doe@example.com',
                    style: TextStyle(fontSize: 18),
                  ),
                ],
              ),

            const SizedBox(height: 40),

            // Two buttons A & B
            Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = true;
                    });
                  },
                  child: Text('A (Show Image)'),
                ),
                const SizedBox(width: 20),
                ElevatedButton(
                  onPressed: () {
                    setState(() {
                      showImage = false;
                    });
                  },
                  child: Text('B (Show Info)'),
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
