Question 06: Build a Flutter widget that displays two text elements in two separate columns. The first text element should say "First Name" and the second should say "Last Name". Make sure the two text elements are evenly spaced and centered within their respective columns. 
source code:
import 'package:flutter/material.dart';

void main() {
  runApp(NameColumnsApp());
}

class NameColumnsApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Two Columns Example',
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(title: Text('Two Text Columns')),
        body: Center(
          child: Row(
            mainAxisAlignment: MainAxisAlignment.spaceEvenly, // Even spacing
            crossAxisAlignment: CrossAxisAlignment.center, // Vertically centered
            children: [
              Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: const [
                  Text(
                    'First Name',
                    style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
                  ),
                ],
              ),
              Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: const [
                  Text(
                    'Last Name',
                    style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
                  ),
                ],
              ),
            ],
          ),
        ),
      ),
    );
  }
}
